{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://free2z.com/schemas/zuuli-build-info-v1.json",
  "title": "ZUULI immutable build metadata",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "channel"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "channel": { "enum": ["internal", "beta", "stable"] }
  }
}
