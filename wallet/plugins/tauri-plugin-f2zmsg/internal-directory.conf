# INTERNAL-DISPOSABLE directory configuration — ADR 0017
# (docs/e2ee/decisions/0017-internal-directory-activation.md).
#
# This file is compiled into every build of `tauri-plugin-f2zmsg`, and so into
# ZUULI (cash.free2z.zuuli) and e2e2z (cash.free2z.e2e2z). It is the ONLY place
# a shipping build learns which key-transparency log, witness, handle authority
# and relay to use. Filling it in is a one-file change.
#
# WHAT THIS POSTURE IS. free2z runs the log AND its only witness. `t = 1` is met
# by that witness, so lookups resolve, and the client reports ZERO independent
# witnesses, so the "not independently witnessed" warning stays on screen. The
# log is disposable: it is wiped before any public launch, and every handle,
# entry and pin made against it dies with it. Nothing below may claim otherwise.
#
# WIPING IT. Give the new log a NEW genesis signing key (so a new `log_id`),
# ship its values here, AND list the old genesis key as `retired_log_public_key`
# below. A device persists the last tree head it verified, sealed under its own
# keys; on the next start that head is resumed when it is this log's, set aside
# when it is a listed retired generation's and really signed by it, and REFUSED
# otherwise — a checkpoint for an unlisted log, or one whose bytes were edited,
# leaves the directory unusable on that device until the user enrolls again. A
# log reset under the SAME key is refused as a rollback for the same reason:
# a reset must be seen, never absorbed.
#
# FAIL CLOSED. While every key and URL below reads PLACEHOLDER the build keeps
# `NoDirectory` and configures no relay — it never connects anywhere. A file
# with SOME placeholders and some real values is malformed, and malformed does
# not compile: `build.rs` parses this file with the plugin's own grammar
# (`src/internal_directory/syntax.rs`) and fails the build of every crate that
# links the plugin. There is no environment override, by design.
#
# Grammar: `key = value`, no quoting, no interpolation. A `#` is a comment ONLY
# as the first non-blank character of a line (leading spaces are allowed) — a
# `#` anywhere else is an error, not a trailing comment, because a URL silently
# cut at its fragment is a different URL. Every value is printable ASCII with no spaces, so a zero-width space or
# a Cyrillic look-alike in a host cannot pass review. Unknown keys are errors.
# Keys are 64 lowercase hex characters (Ed25519 public keys).
#
# Filled in by the deployment workstream (zuu#1022, workstream 9) from the
# values the deployed services publish:
#
#   log_url               the log's public origin, https:// (TLS terminates ahead
#                         of f2z-kt, see rs/crates/f2z-kt/src/api.rs)
#   log_public_key        the log's GENESIS signing key
#   log_id                BLAKE2b-256("free2z/kt/v1/log-id" || log_public_key)
#                         (KT.md §6.1), as the log publishes it. Checked
#                         against the derivation, so a pasted key and id from
#                         two different logs are refused
#   vrf_public_key        the log's ECVRF public key, as its tree heads carry
#                         it. Pinned: a first head naming another key is refused
#   reset_authority_pk    the log config's `reset_authority_pk` (ADR 0014),
#                         pinned here because a key learned from the log is no
#                         authority at all
#   reset_cooldown_seconds  the log config's `reset_cooldown_seconds`. At least
#                         604800 (seven days): this client enforces the value
#                         on every platform reset it sees, and ADR 0014's
#                         window only protects a user who opens the app inside
#                         it. A smaller number is a code change
#   witness_pk            the witness's public key (`f2z-witness`); repeatable.
#                         Every witness here is counted as NOT independent —
#                         this file cannot assert independence (KT.md §8.3)
#   threshold             t; at least 1, at most the number of witness_pk lines
#   handle_authority_pk   the backend's HandleAssertion signing key — the same
#                         value as the log config's `authority_pk` (Contract C).
#                         It must be the ONE authority the log's signed policy
#                         lists (KT.md §4.6): a bundled build refuses a log
#                         that does not vouch, or vouches with another key
#   handle_assertion_url  the backend endpoint that issues HandleAssertions,
#                         https:// (ADR 0017 §5); for production
#                         https://free2z.cash/api/kt/handle-assertion/
#   relay_url             the default relay, wss://…/relay/v1
#   retired_log_public_key  a genesis key of a log generation this build has
#                         LEFT (see WIPING IT); repeatable, and absent until
#                         the first wipe. Naming one is what lets a device set
#                         that generation's checkpoint aside instead of
#                         refusing it

posture = internal-disposable

log_url = https://kt.free2z.cash
log_public_key = 650b5cadbe37ad0054bbcfd77e202917318ca3fd847ac835f810bfb41c913e61
log_id = 05b5dc5aa07ecae55442b8b32b6b8bebcc6490031112caa30adefa84070dc7e9
vrf_public_key = 6b86eeff3c36a159f2841b16b73568bc85f60ba6aa7a0478eff2d3d2ac4b7706
reset_authority_pk = 98d24429bc467cd94f2d4863ae84d50d5321c93030389bc144fd75f1afb34db3
reset_cooldown_seconds = 604800
witness_pk = 98868a0c8239837425f4fbb9e5293691940457d5fd0ccd4aff465701f8669518
threshold = 1
handle_authority_pk = 332e237e2f9db842905c2a6011f4d306824b2f33eef01686d5b29d69e843934f
handle_assertion_url = https://free2z.cash/api/kt/handle-assertion/
relay_url = wss://relay.free2z.cash/relay/v1
